// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: 'AIzaSyBy98SWcFIAl44A3SlXSEJYSYqTsoZB4Qg',
    authDomain: 'shopapp-f2098.firebaseapp.com',
    databaseURL: 'https://shopapp-f2098.firebaseio.com',
    projectId: 'shopapp-f2098',
    storageBucket: 'shopapp-f2098.appspot.com',
    messagingSenderId: '1006583943379',
    appId: '1:1006583943379:web:05643c333de69935',
    // Initialize Firebase
    // firebase.initializeApp(firebaseConfig)
  },
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
